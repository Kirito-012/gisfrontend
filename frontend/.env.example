# Base URL of the deployed backend API (no trailing slash).
# Leave empty for local dev (Vite proxies /api to :8000) or single-process hosting.
# Example for split hosting:  VITE_API_BASE=https://thecraftsync-api.onrender.com
VITE_API_BASE=
